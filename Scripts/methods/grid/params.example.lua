-- Example for the "grid" method.

return {
    -- Location of the "actor reference". Press F5 (default) on an object in game to define it.
    ACTOR_REF_LOC = nil,

    -- The arc length is the length between two objects in the grid.
    -- Orange arc circles should always have a length of ARC_LENGTH (see the GeoGebra simulation).
    ARC_LENGTH = 500.000000,

    -- The angle defines grid orientation.
    -- It corresponds to the planeAngle variable in the GeoGebra simulation.
    ANGLE = 90.000000,

    -- An rotation that will be added to the object.
    ROTATION_ANGLE = 0,
}
