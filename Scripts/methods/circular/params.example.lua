-- Example for the "circular" method.

return {
    -- Location of the "actor reference". Press F5 (default) on an object in game to define it.
    ACTOR_REF_LOC = nil,

    -- Forward direction of the "actor reference". Press F5 (default) on an object in game to define it.
    ACTOR_REF_FORWARD = nil,

    -- The arc length is the length between two objects on the same longitude.
    ARC_LENGTH = 500.000000,

    -- Angle between each meridian.
    LONGITUDE_ANGLE = 10.000000,
}
